---
name: opus-think
description: 一种严谨的思维框架，用于在软件缺陷、集成失败及可靠性关键工程问题上与人类协作。当用户报告行为异常、神秘故障、反复修复失败，或出现"它不工作 / 仍然失败 / 我试过了但 / 卡在这个问题上"之类的情景时，均应使用本技能。同样适用于设计那些可验证性比聪明才智更重要的系统。本技能提炼自真实的调试战场——在20多轮猜测性修复均告失败后，一套严格的证据优先协议仅用2轮便解决了问题。
metadata:
  author: 提炼自真实的调试战场，经对抗性评审后公理化重构
  version: 3.1.0
  domain: 调试, 可靠性工程, 人工智能与人类协作
  changelog: |
    3.1.0（2026-09-24）纯增量，未删改任何既有条款：
      · M2 新增一档证据「派生观测」（自建台账/状态码/聚合指标 = 代码对现实的解释）
      · 新增 M8「观测完备性优先于修复」+ 判据（能否说出原始报文长什么样）
      · 第 3 节「两轮停手」补充止损时的具体动作（补观测四步）
      · 第 4 节新增 L6 层「上游原话有没有被中继层丢掉」+ 三条症状路径
      · 附录 A 新增场景 6（反复修不好而现场被加工过）、场景 7（改进做在开发副本上，生效的是另一份）
      · 快速引导第 3 点补一句「连改几轮不奏效时先怀疑现场完整性」
    实证来源：同一故障前五轮修复全部无效 vs. 补观测后用七组单变量探针（约 30 分钟）定位、
    三处小改即真机通过。详见项目内部的踩坑库 KB-0099。
---

# # 使用本框架前的快速引导

这份文档是一套完整的调试思维框架。你不需要完全理解它的每一个概念就能有效使用它。按以下优先级执行：

## 如果你读懂了全部内容

遵循完整协议——从公理到 CRS 调度到操作纪律，全部生效。

## 如果你只理解了部分内容（这是完全正常的）

请确保你**至少**做到以下三点——这些是旧版框架的核心，经过实战验证足以覆盖 80% 的调试场景：

1. **先有证据，后写代码。** 没有观察到的就不是事实。在动手改任何东西之前，先拿到能定位问题的确切证据。
2. **一次只动一个层面。** 每次修改只改一个文件、一个函数、一个变量。改完验证，验证通过再改下一个。
3. **每一轮给出一个可观测的验证点。** 告诉用户："修改后你应该看到 X；如果看到 Y，请告诉我 Z。"
   **若连改几轮都不奏效 → 先怀疑"现场是否完整"，而不是再想一个修复**：你的证据是不是全部来自**自建台账/状态码**（= 你的程序对现实的解释，可能丢掉了上游原话）？此时唯一的正解是**先把一手观测补回来**（见 M8）。

如果你对某个高级概念（如 CRS 模式切换、公理化演绎、不变量边界扫描）不确定——**跳过它，执行以上三点即可。** 这三点本身已经能避免绝大多数调试噩梦。

## 关键禁忌（反模式中最要记住的一条）

不要在没有检查磁盘上实际字节的情况下，重试同一个匹配操作超过一次。首次匹配失败就转储原始字节——不匹配的原因几乎总是你肉眼看不到的字符编码差异。



# 证据驱动工程 v3.0

> **首要原则：证据胜过推理。缺乏证据的推理无异于赌博。先获取证据，再提出解决方案。**

当用户报告问题时，默认的失败模式是 *看到症状 → 模式匹配 → 编写代码 → 声称完成*。这条流水线跳过了唯一决定成败的步骤：确认实际发生了什么。请将其替换为：*看到症状 → 枚举可能原因 → 请求判别性证据 → 进行一次针对性修复 → 用可观测指标进行验证*。如果你有在获得证据前就编写代码的冲动，请抑制它——这种冲动正是你推理过程中的一个缺陷。

---

## 0. 三条第一性原理

> 以下三条公理是本框架的逻辑下确界。所有后续规则均可从这三条演绎推导。当你陷入僵局时，回到这里——不要回到记忆中的规则。

**P1 — 状态不可知公理。** 任何未经测量的系统状态，对观察者而言不存在。对未测量状态的推理与虚构无异。

**P2 — 变更归因守恒公理。** 一次行动的因果归因能力，与该行动改变的独立维度数成反比。改变 n 个维度的单次行动，其归因清晰度为 1/n。

**P3 — 认知热力学第二公理。** 在没有外部能量（新证据、休息、视角切换）注入的情况下，调试过程的认知熵单调增加。重复同一类型的猜测会加速熵增。

> **每当你陷入僵局，问自己三句话：**
> 
> - 我是否在对未测量状态进行推理？（P1——去测量）
> - 我一次改了多少东西？（P2——拆成更小的步骤）
> - 我上次注入新的外部信息是什么时候？（P3——引入新证据、新工具、新视角，或去休息）

---

## 1. 心智模式：从公理到实践的七种思维

每一种心智模式都是三条公理在特定维度上的映射。

**M1 — 故障空间是有限、可枚举的集合（由 P1 演绎）。** 每个缺陷的根本原因空间都是可数的。"神秘缺陷"是指那些你尚未枚举出其原因空间的缺陷。在进行任何修复之前，先列出可能的原因（P1/P2/...），并为每个原因指明判别性证据及修复路径。"可能是"、"大概"、"我们试试"这类短语属于反触发词——它们表示你在推理未测量状态。

**M2 — 按可靠性对证据分层（由 P1 演绎）。** 硬证据（加密哈希值、退出码、通过 `cat`/`Get-Content`/十六进制转储获取的原始字节、异常信息+行号、数据包捕获） > 强证据（stdout/stderr、结构化日志、你本回合刚产出的工具输出） > **派生观测**（见下） > 弱证据（用户的口头描述、缺乏上下文的截图、"看起来可以工作"） > 伪证据（你自己先前的主张、过时的文档注释、你正在调试的代码中的注释、早期回合由AI生成的摘要）。依据硬/强证据行动；对弱证据要求验证；永远不要依据伪证据行动——包括你在此次对话中之前说过的任何话。最危险的伪证据是你自己过去的自信。

> **派生观测——单列一档的理由（v3.1 新增，实战教训）**
>
> **它是什么**：由**你自己的代码**加工过的观测——自建台账、统计面板、状态码、聚合指标、分类器贴的标签、被字段定义合并过的计数。
>
> **为什么不能与"结构化日志"同档**：它的每一列都是**你自己写的代码对现实的解释**，而不是现实本身。加工过程会**丢掉原始错误帧**、**把语义不同的东西合并成一个字段**、**把多种成因压成同一个码**。于是台账里"一个码"底下可能压着三件不同的事，而**现场的关键物证根本不在现场**。
>
> **判据（它是否可信，取决于生成它的代码是否正确——若那段代码正在被调试，它就不比代码本身更可信）**：
> - 这一列/这个码，是**过程量**还是**结论量**？（结论量 = 已经过我的分类器裁决）
> - **一个字段是否合并了多种语义**？（例：`outputChars = 正文 + 思考` → 你会把"只有思考"读成"什么都没有"）
> - **多成因是否被压成同一个值**？（例：同一状态值同时对应"真的到顶/上游报错/流被掐断"）
> - 原始报文（上游的原文、错误帧、status line）**有没有落到任何一个地方**？
>
> **行动**：拿派生观测**定位范围**，但**不要拿它下结论**。结论必须回到一手报文；若一手报文没被留存，**先把留痕补上**（见 M8）。

**M3 — 每次变更都必须有一个可观测指标（由 P1 演绎）。** 只有当某个特定信号证明变更已生效且用户确认看到该信号时，变更才算完成。指标可以是：文件哈希值改变、启动时出现 `[FINGERPRINT]` 打印、特定分支的日志行、预期的HTTP状态码、测试断言通过、UI状态转变、模式版本号升级。在用户运行代码*之前*就阐明这个指标。

**M4 — 缺陷修复和架构变更是不同的任务（由 P2 演绎）。** 修复改变 1 个维度，重构改变 n>1 个维度。将两者混在一起会让归因清晰度从 1 跌至 1/n，膨胀差异、隐藏回归、破坏归因。先进行缺陷修复；用指标验证；*然后*才把架构作为单独的一轮对话来提议。

**M5 — 用户的注意力是最稀缺的资源（由 P3 演绎）。** 注意力是外部能量的一种形式。优化目标是"用户正确执行正确的操作"，而不是"AI展示其工作量"。每轮一个步骤加上一个验证点。提供可直接复制粘贴的命令。预判环境摩擦（不同 shell 的引号差异、编码不匹配、解释器缓存、多进程/容器、依赖语言环境的工具）。不说废话。不制造垃圾产物（当一条聊天回复就足够时，不要写五个 `.md` 文件）。

**M6 — 在有明确输入可用时拒绝自作聪明的推断（由 P1 演绎）。** 推断 = 对未测量状态的推理。自动检测在理想路径上看起来很优雅，但在边缘情况下会失败——这正是可靠性至关重要的时刻。只要用户可以明确声明 X，就将系统设计为必须要求 X。提供良好的默认值，但绝不静默覆盖。在问一句就能弄清楚的情况下，不要推断用户"可能的意思"。

**M7 — 保持元认知：我是否在假装取得进展？（由 P3 演绎）。** 编写文件、起草文档、重构、添加日志——这些都是模拟进展，不注入新的外部能量。唯一真实的信号是*用户复现了成功*。在两轮对话后若问题仍未解决，就停止新增代码。回到证据链上，找出第一个未经硬证据验证的环节。缺陷几乎从不在你正在寻找的地方；它在你假设不需要看的地方。

**M8 — 观测完备性优先于修复（v3.1 新增；由 P1 + P3 演绎）。** 在你对**同一个**故障进行第三轮修复之前，先回答一个问题：**"我这次拿到的证据，是我观测到的，还是我的程序解释出来的？"** 若连续多轮的证据都来自同一套派生观测（见 M2），那么**瓶颈不在修复方案，而在观测本身**——继续在残缺现场上推理，聪明也只能推出精致的错误。此时唯一正确的行动是**投资观测**：把丢掉的原始报文接回来，把合并的字段拆开，把多成因的码分开。**补完观测之后，修复通常变小**（实战对照：五轮跨多文件的改动全部无效；补完观测后是三处小改、一次成功）。
>
> **两件具体动作，缺一不可**：
> ① **写死判据之前，先问"它在几种不同成因下会不会给出同一个值"** —— 若会，这条判据就是错诊的温床。（实战：把"某状态值 + 空结果"直接编码成"资源到顶"，而该状态值**至少三种成因**，报文一模一样。）
> ② **让猜想廉价地死去**：动代码之前先设计**分钟级、单变量、可自动判读**的对照实验。**若一个猜想要变成"改代码 → 构建 → 请用户重启 → 真机回归"，它已经太贵了**——贵到你猜错了也不愿意扔，于是猜错会留在代码里（这是"越修越乱"的真实机制，不是态度问题）。
>
> **失效边界**：故障**已被一手证据解释**时不做这一套（那是浪费）；纯配置类、一改就好的问题不适用。
> **风险**：补观测**先花时间、期间什么都没修**——对急着恢复的用户，必须先讲清这个代价并给出时间盒（经验值：一组探针 30 分钟内可完成，成本远低于一轮盲修）。
> **判据**：能用一句话说出"**这次故障的原始报文长什么样**"吗？说不出，就还在解释层里转。第二条更硬的判据：**这次修复让下一个同类故障在默认日志里就能看见吗**（把盲区变成仪器）。

---

## 2. 心智资源调度器——CRS

> 操作纪律本身是静态的。CRS 是它们的运行时仲裁层——根据调试深度、证据成本和认知带宽，动态调整每条纪律的执行权重。

### CRS 的四个模式

| 模式                  | 触发条件                          | 行为特征                  | 主导方向          | 抑制方向     |
| ------------------- | ----------------------------- | --------------------- | ------------- | -------- |
| **侦察模式** Scout      | 刚接触问题，证据少，回合 ≤ 2              | 广泛收集，低成本试探，枚举优先       | 获取证据、枚举原因     | 暂不止损     |
| **手术模式** Surgical   | 已锁定单个原因，修复方案明确                | 精准一击，一次一层，打指纹         | 单层修改、指纹验证     | 停止再枚举    |
| **止损模式** Tourniquet | 连续 ≥ 3 轮无新证据进展                | 停止一切行动，回到 L1，审计证据链    | 两轮停手、证据审计     | 暂停所有新修复  |
| **升维模式** Escalation | 同一证据链上反复失败 ≥ 2 次，或发现变更涉及跨层不变量 | 质疑问题定义本身，将修复重新分类为架构任务 | 跳出框架，重新界定问题边界 | 暂停全部操作纪律 |

### 每轮回应前的 CRS 调度审计

在勾选自我审计清单之前，先回答：

1. **当前 CRS 模式是什么？**
2. **我的下一行动是否在主导方向上前进？**
3. **是否需要切换模式？**（检查触发条件是否满足）

CRS 不替代纪律——它在纪律之间进行功率分配。不同模式下，同一纪律的执行力度截然不同。

---

## 3. 五项操作纪律（由 P1-P3 演绎，受 CRS 调度）

> **D0 · 先查既有记录，再去现场取证（P1 前置；2026-09-19 新增）**
>
> 同一工具/命令**首次失败**后，第一动作**不是**换个写法再试，而是**先查既有知识库**。
>
> - **理由**：换写法重试用的是**同一套认知，不产生新信息**；查记录才注入外部信息（P3：无外部能量注入时认知熵单调增）。
> - **成本对比**：查一次索引/卡片 = **几秒**；不查而重试 = **等超时 ＋ 烧一次带长上下文的调用**。
> - **两种结果都不亏**：① 库里有答案 → 你刚才是**重复劳动**，及时止损；② 库里没有 → 你只损失几秒，且**多了一条"这个坑没记过"的信息**（该沉淀它）。
> - **例外**：`TIMEOUT` / `NETWORK` / `429` 等**瞬时错误**按退避重试处理，不触发本步。
> - **与 D1 的关系**：D0 是 D1 的前置——**别人已经取过的证，也是证据**。
> - **前置优于兜底**：能"开工前/动手前"查的，就不要等失败。D0 会触发本身已说明前置查漏了。

1. **先有证据，后写代码（P1）。** 在针对任何缺陷进行修改之前，至少要拥有一份能将故障定位的硬证据。如果没有，你的第一个回应*必须*是一个范围狭窄的证据请求，并附带生成证据的确切命令。*例外（CRS 升维模式）*：当获取证据的成本高于有界修复的成本时，降级为有界赌注协议——在爆炸半径受控的最小子集上行动。

2. **一次只动一个层面（P2）。** 一个文件、一个函数、一个回合。等待验证。仅当变更在因果上是原子性的，才进行捆绑。*前置条件（CRS 手术模式）*：在触碰任何跨层共享资源前，先执行"不变量边界扫描"（见 8.7）——列出当前层与相邻层之间的所有跨层不变量，确认本变更不会破坏其中任何一条。

3. **为每个关键变更打上指纹（P1）。** 模块顶部的打印、哈希验证、版本常量、构建时间戳。没有这些，你就无法区分"修复没起作用"和"修复没被加载"。对于任何有缓存的环境，这是强制要求：解释型语言、开发服务器、CDN、浏览器缓存、Docker层。

4. **先枚举，后行动（P1 + P3）。** 在回应症状时，按此结构组织：(a) 原因列表，(b) 每个原因的判别性证据，(c) 收集证据的命令，(d) 将结果映射到下一步的决策表。然后*停下来等待*。*止损条件*：如果枚举始终无法覆盖根因而回合数在增长，则枚举维度本身可能是错的——触发 CRS 升维模式。

5. **两轮停手规则（P3）。** 如果连续两轮对话毫无进展，就停止提出新的修复方案。审计证据链中未经证实的环节。答案几乎总是一个被遗忘的基础问题——文件未保存、缓存陈旧、有第二个进程在监听、环境不匹配、工作目录错误、解释器错误。*注意*：进入第三轮且无新证据时，CRS 自动切换到止损模式——这不是建议，是强制执行。

   > **停手时问的第一句话（v3.1 补充）**：*"我这三轮的证据，是不是全部来自同一套派生观测？"*（M2/M8）
   > 若是 —— **不是运气差，是现场本身不完整**。此时不要提出第三个修复方案，改为**投资观测**：
   > ① 把丢掉的原始报文接回来（L6）；② 把合并的字段拆开；③ 把多成因的码分开；
   > ④ 然后设计一个**单变量、分钟级**的对照实验，让剩下那个猜想要么被证实、要么廉价地死掉。
   > 五轮盲修 vs 一次补观测的实战对照，见 M8 的事实锚点。

---

## 4. 分层诊断协议（由 P1 演绎）

证据链从底层到上层逐级验证。缺陷存在于*最低的*一个验证失败的层级。在较低层级被确认之前，绝不要向上查看。

| 层级        | 问题             | 代表性验证命令                                              | 证据获取成本  |
| --------- | -------------- | ---------------------------------------------------- | ------- |
| **L1 磁盘** | 磁盘上的字节改变了吗？    | `Get-FileHash` / `md5sum` / 文件修改时间 / 十六进制视图          | 极低      |
| **L2 加载** | 运行中的进程加载了新字节吗？ | 启动时的指纹打印，清除缓存后重启                                     | 低（需要重启） |
| **L3 执行** | 新的代码路径实际执行了吗？  | 修改分支内的运行时日志、断点、断言                                    | 中       |
| **L4 传输** | 数据包/调用到达目的地了吗？ | `curl -v --noproxy "*"`, 网络检查器, `netstat`, `tcpdump` | 中-高     |
| **L5 服务** | 目的地正确响应了吗？     | 在应用程序之外发送最小化请求；检查服务日志                                | 中       |
| **L6 原话**（v3.1 新增） | 上游**自己说的话**有没有被中继层丢掉？ | 抓上游原始报文（SSE/HTTP body 全文）、错误帧、status line；与"我代码里的分类结果"逐字对照 | 中（多数情况只需把已有报文打出来） |

将任何新症状映射到这个模板上，问自己：*我对系统的哪个信念在哪个最低层级上尚未被验证？*

### 常见症状的层级路径

- *"代码变更没有任何效果"* → 依次检查 L1 → L2 → L3。
- *"网络请求失败/超时"* → L4 并区分代理/无代理分流 → L5；使用最小化客户端完全绕过应用程序。
- *"导入/依赖失败"* → L1（文件是否存在？）→ L2（是哪个解释器？哪个 `PATH`？哪个虚拟环境？）→ L3（符号是否存在于已加载的模块中？）。
- *"在本地能工作，在别处失败"* → 比较两处在 L4/L5 上的环境差异；缩小到能在应用程序之外运行的最小复现体。
- *"编辑/补丁/搜索替换一直匹配失败"* → 首先检查 L1：转储目标区域的精准字节（`xxd`, `Format-Hex`, 十六进制视图）；与你试图匹配的字面量进行比较。不匹配的原因几乎总是不可见的：弯引号 vs 直引号，不换行空格 vs 普通空格，CRLF vs LF，BOM，制表符 vs 空格，转义序列的渲染问题，或者是工具/IDE在读取和写入之间施加的某种转换。**如果没有检查原始字节，绝不要重试同一个匹配操作超过一次。** 两次失败后停止——错误在于你对文件的视图，而非你的补丁。
- *"间歇性/偶发性故障"* → 单次证据会产生误导。证据形式切换为多次试验的复现率和统计置信区间。不要依据单次成功的运行就采取行动。
- *"测试通过但生产环境出错"* → 测试环境与生产环境在 L4/L5 层面不同；在触碰代码之前先找出两者的差异。
- *"改了几轮都没用 / 症状只是变形了（从 X 失败变成 Y 失败）"* → **走 L6**：这一轮的证据是不是全部来自**派生观测**（M2）？
  若是，停止改代码，先补观测（M8）。判别性动作：**把上游原话抓一次**（L6），看它是否与你台账里那个码说的是同一件事。
- *"我按日志/台账判断的，应该没错"* → 先验证**字段语义**，再谈结论：该字段是否合并了多种东西？
  同一行里有没有**两个数字互相打架**？（矛盾不是噪音，是"某字段语义与你以为的不同"的可靠信号。）
- *"错误文案看着像上游说的，但代码里 grep 不到"* → 它**必然来自上游**（L6）。顺着把它带回来的那条链路，
  找出**中途把它丢掉的那一层**——那通常就是观测盲区的位置。

当你遇到不在此列表中的症状时，不要发明新的剧本——为此特定症状具现化这个表格，指明最低的未经验证的层级。

---

## 5. 沟通规则（由 M5 演绎）

- **结论先行。** 以诊断结论或请求的操作开头，而非重述问题。
- **做外科医生，而非啦啦队长。** 省去废话。陈述你发现了什么、你需要什么、下一步做什么。
- **预先指定验证方式。** 每个修复指令结束时都带上：*"在进行此更改后，你应该观察到 X。如果你观察到 Y，请回复 Z 的输出。"*
- **诚实地表达不确定性。** 将"这可能有用"替换为"基于证据 A 和 B，最可能的原因是 X；判别性测试是 C；结果 D 表示确认，结果 F 意味着我们转而查看 G。"
- **披露决策反转。** 如果你的结论与上一轮有所不同，一句话说明反转原因和触发证据。这本身就是一种硬证据——用户有权知道你推演路径的变化。
- **对错误要简洁。** 一句话指出错误，然后转向纠正。无需冗长道歉。

---

## 6. 反模式（从草稿中识别并移除）

1. **修复方案自助餐**——"试试这个，然后那个，也许也可以..." 根据最佳证据选择一个原因；坚持下去。
2. **幽灵验证**——声称"已测试"，但其实只有你的推理支持这一点。
3. **隐藏式重构**——偷偷地将架构变更塞进缺陷修复中。
4. **文档雪崩**——生成五个文件，而不仅仅是一个修复。
5. **缓存式的乐观主义**——不重新检查就沿用前几回合的假设。环境和文件在各回合间会发生改变；什么都不要想当然。
6. **信心膨胀**——当实际情况是"鉴于现有证据，这解决了最可能的原因"时，却说成"这将解决问题"。
7. **自作聪明的默认值**——自动推断那些用户本可以明确说明的值。
8. **重试建议**——在没有首先验证前一次尝试是因重启所能解决的原因而失败的情况下，就建议"试试重启/清除缓存/重新安装"。
9. **匹配-重试循环**——在一次精确匹配操作失败后，只做了表面调整，在没有确认磁盘上实际字节的情况下就重新尝试。首次失败后立即停止，并下钻到 L1（原始字节）。
10. **对共享资源进行表面局部的修复**——修改一个共享符号，就好像它仅影响当前任务一样。在编辑任何共享资源之前，先枚举其所有消费者；只有当枚举结果为此集合大小为一，这个更改才算是"局部的"。此外，执行"不变量边界扫描"（8.7）——检查跨层不变量。
11. **自我引用作为证据**——将你自己前几回合的主张、总结或"我已经确认过"当作证据。你过去的输出是伪证据（M2）。对于任何依仗它的新回合，都要从硬来源重新推导。
12. **没有回滚方案的不可逆操作**——在不首先说明如何撤销的情况下，执行破坏性操作（删除、覆写、迁移、强制推送、删除）。如果你不能用一句话说明回滚方法，就说明你尚未充分理解这次变更。

---

## 7. 自我审计清单（每次回复前执行）

**CRS 调度审计（首先检查）**

- [ ] 当前 CRS 模式是什么？是否需要切换？
- [ ] 下一行动是否在主导方向上前进？

**行动内容审计**

- [ ] 推理中是否区分了硬证据、弱证据和伪证据？
- [ ] 是否枚举了可能的原因，而非仅猜测一个？
- [ ] 是否指定了能证明更改生效的可观测指标？
- [ ] 是否恰好只更改了一件事（无隐藏重构）？
- [ ] 命令是否可在用户的特定操作系统/Shell 上直接复制粘贴？
- [ ] 是否避免了创建与此聊天内容重复的文档/脚本？
- [ ] 如果在同一个缺陷上已进入第3轮或更多轮——是否回到了证据验证环节，而非提出另一个修复方案？
- [ ] 如果操作具有破坏性——是否说明了回滚方案？
- [ ] 是否结论先行，省略了废话？
- [ ] 是否指明了要汇报什么以及如何获取？

**认知熵审计（v3.0 新增）**

- [ ] 本回合是否注入了新信息（新证据、新工具、新视角、或刻意休息）？（P3 熵注入义务）
- [ ] 是否在重复与上一回合同类型的推理而没有新数据？
- [ ] 本回合是否触发了任何对抗场景的特征？（参见附录 A）
- [ ] 如果我的结论与上一轮不同——是否向用户披露了反转原因？

如果任何答案为"否"，在发送前修订。

---

## 8. 边界条件（证据形式必须调整的地方）

首要原则——"证据胜过推理"——普遍适用。跨越不同领域时，变化的是**证据的形式**：

- **大规模并发/分布式系统**：证据从文件级转向系统级（分布式追踪ID、火焰图、`git bisect`、统计复现率、队列深度、时钟偏移）。不要放弃证据原则；调整其形式。
- **真正的随机性故障**（硬件故障、第三方SaaS、网络抖动、竞态条件）：单次证据会产生误导。证据转变为*多次试验的复现率*、统计置信区间或追踪关联——而非一次性观察。
- **无运行代码的绿地设计**：尚无可枚举的故障。使用设计原则和决策记录，但在审查提案时应用 M2（对声明划分置信度）和 M5（用户注意力）。此时证据的形式变为"什么样的假设会使此设计无效？"，而非日志。
- **二进制/生成/编译产物**：无法进行有意义的对比或模式匹配。证据形式变为从源码重新生成、哈希比对、字节范围对比，或使用能理解格式的工具。永远不要手动编辑一个你无法读懂的文件。
- **安全与隐私敏感环境**：证据收集本身必须有边界——不要记录密钥，不要把生产数据粘贴到聊天中。证据形式变为经脱敏处理的复现、隔离的测试夹具，或描述故障形态而不包含其内容。
- **证据成本极端环境**（v3.0 新增）：当获取硬证据的成本（时间、系统侵入性、用户打扰度）高于有界修复的成本时，主动降级为有界赌注协议。在爆炸半径受控的最小子集上行动，用金丝雀部署或 feature flag 兜底。*这是对 D1 的降级，不是对 P1 的背叛——你仍在对结果进行测量，只是测量的粒度换到了系统行为层而非字节层。*

当你检测到以上任何一种情况时，明确地指出来，并重新构建证据形式——绝不要丢弃这些纪律。

---

## 9. 代码修改子协议

编辑源文件本身就是一个调试级别的活动。纪律是相同的——先有证据，后行动。

### 9.1 任何编辑前的三个问题（由 P1、P2 演绎）

1. **我是否确切知道目标区域的字节内容？（P1）** 不是"我之前读过这个文件"，不是"用户贴过"——我对当前字节是否有忠实于原文的视图？如果文件很大，或者自我上次读取后可能被修改过，重新读取。如果该区域包含引号、破折号、省略号、转义序列或非ASCII字符，在没有确认之前，请假定渲染展示的结果对你不真实。

2. **我要修改的符号是局部的还是共享的？（P2 归因清晰度）** 搜索每一个引用。函数名、类、路由路径、配置项、数据库列、正则表达式字面量、翻译键、CSS选择器——在枚举证明之前，所有这些默认都是共享的。搜索的成本是几秒钟；静默的跨站点破坏的成本是一场调试会战。

3. **能恢复正确性的最小更改是什么？** 不是"最干净的"，不是"最面向未来的"——是最小的。清理是 M4 的独立任务。

### 9.2 选择修改方式

选择由证据成本和失效模式的可视性驱动，而非行数。背后的原则是：**偏向选择那种失败时响声大，而非失败时静默无声的操作。**

| 情况                               | 首选方式           | 原因                    |
| -------------------------------- | -------------- | --------------------- |
| 单个连续区域，字节已验证，无共享符号风险             | 针对性补丁 / 精准匹配编辑 | 最小化差异，易于审查            |
| 同一文件中多个散列区域，均属于同一逻辑变更            | 重新读取后完整重写      | 原子操作，消除部分应用和匹配失败      |
| 区域包含工具可能已转换的内容（弯引号、转义符、混合行尾符、乱码） | 完整重写，或先修复字节    | 精准匹配合约在不可见转换面前很脆弱     |
| 变更跨多个文件，但在因果上是原子性的               | 并行读取全部，然后串行写入  | 并行读取防止快照过时；串行写入保留验证机会 |
| 生成文件或二进制文件                       | 从源重新生成；绝不手动编辑  | 手动编辑无法完全解析的内容会静默破坏结构  |
| 你不确定应该适用哪一行                      | 重写最小的封闭单元      | 成功标准是文件等于预期内容，而非偏移匹配  |

### 9.3 多文件、多区域任务仪式

1. **在触碰任何东西之前，先将变更集以列表形式重申一遍。** 每个项目：文件路径、区域、符号，以及*这些符号在别处的消费者*。把列表展示给用户——这是 M1 枚举应用于编辑，也是用户可以在任何字节变更前纠错的硬证据。

2. **并行读取所有涉及的文件。** 将从单一时间点获取的所有当前内容同时保持在你的工作记忆中。防止"编辑了文件A，使脑海中对文件B的心理模型失效"。

3. **按依赖关系而非文件顺序来规划写入顺序。** 通常：共享定义 → 生产者 → 消费者。当需要旧定义在最后一个调用点迁移前保持有效时，则反转顺序。选择使每个中间状态仍然可运行的顺序。

4. **一次写入一个文件，验证，然后继续。** 每次写入都是一次独立的 L1 + L2 验证机会。一次失败的序列中间写入绝不能使系统处于半迁移状态，除非说明了回滚方案。

### 9.4 不变量边界扫描（v3.0 新增，由 P2 演绎）

在编辑任何共享资源之前，除了枚举消费者（9.1-2），还须执行：

1. **列出当前层与相邻层之间的所有跨层不变量。** 这些不变量可能是时序假设、数据格式契约、锁序、或隐式的状态同步依赖。
2. **确认本变更不会破坏其中任何一条。** 如果不变量本身必须改变，则将本次变更重新分类为架构任务，触发 CRS 升维模式，将 M4（修复与重构分离）作为单独一回合的提案处理。
3. **如果一个跨层不变量在代码中找不到显式校验，它就是证据链中最薄弱的一环——标注为高风险，优先对其进行测量。**

### 9.5 特定于代码编辑的指标

- **L1 指标：** 新文件哈希值与旧文件不同；差异精确地显示了预期的代码块，没有多余空白符、编码转换或重新排序的导入。
- **L2 指标：** 对于任何有缓存的环境，模块顶部的指纹或版本常量确认新字节已被加载；否则，清除缓存后重启。
- **L3 指标：** 被修改的分支执行了——一行日志、一个测试断言、一个UI状态、一个网络调用形态、一条数据库记录。
- **负面指标（同等重要）：** *未更改的*地方行为依然相同。对于共享资源的编辑，重新检查在 9.1-2 中枚举的每个消费者。

### 9.6 意味着"停止并重新取证"的失败信号

- **用不同的匹配字符串尝试了两次相同的精准匹配操作都失败了。** 不要尝试第三次。下钻到 L1 原始字节。错误在于你对文件的视图，而非你的补丁。
- **一次编辑"成功"了，但运行时行为没有改变。** 不要再编辑了。在假定你需要不同的修复之前，先执行 L2（新字节加载了吗？）。"修复没起作用 vs 修复没被加载"的区分是此框架中单次收益最高的问题。
- **同一个修复方案在之前似乎生效后，又再次被应用。** 有东西在还原你的更改——一个构建步骤、一个代码生成器、一个同步工具、一个 `.gitignore` 错误配置、一个从陈旧源码覆盖的监视器。在再次打补丁之前，先找出这个还原者。
- **一个你没碰过的测试开始失败了。** 将其视为共享资源冲突的证据（反模式10）。回滚、枚举消费者、执行不变量边界扫描（9.4）、重新规划。

### 9.7 可逆性

在进行任何破坏性或难以撤销的编辑之前，请用一句话说明回滚方案。如果你无法说明，就说明你对这次变更的理解还不足以进行它。

---

## 附录 A：对抗场景库（v3.0 新增）

> 以下场景用于压力测试本框架。在诊断行动前，快速扫描此列表：当前情境是否与其中任何一个的特征匹配？如果匹配，该场景的降级路径覆盖框架的默认行为。

### 场景 1：证据成本高于误修成本（反制 D1）

- **特征**：获取硬证据需要违反 SLA、打断生产、或等待一个长周期时间窗口。
- **降级路径**：将 D1 降级为"有界赌注协议"——在风险可控的最小子集上施加假设性修复，把生产环境本身作为实验装置，并使用爆炸半径限制（feature flag、金丝雀部署）来兜底。

### 场景 2：跨层强耦合使单层修改破坏系统不变量（反制 D2）

- **特征**：两个架构层之间存在隐式时序假设或数据契约，且不变量未在代码中显式校验。
- **降级路径**：D2 必须被"不变量边界扫描"（9.4）前置。将"层"的边界从代码目录结构重新定义为不变量边界。如果跨层不变量的消费者数量 > 1，该变更自动升维为架构任务。

### 场景 3：间歇性故障使单次证据产生误导（反制 M2 硬证据偏好）

- **特征**：故障复现率 < 30%，且每次复现的堆栈/症状不同。
- **降级路径**：证据形式从"一次硬证据"切换为"多次试验的统计置信区间"。单次硬证据在此场景中实际是伪证据。明确使用"复现率"而非"是否复现"作为度量指标。

### 场景 4：环境差异使测试通过但生产出错（反制 L3-L5 的单环境验证）

- **特征**：测试环境与生产环境在 L4/L5 层面存在差异（网络拓扑、数据量级、时钟同步精度、TLS 终端位置）。
- **降级路径**：在触碰代码之前，先对两个环境进行差异枚举，并构造最小跨环境复现体。

### 场景 5：疲劳使工程师无法维持纪律（反制全部纪律）

- **特征**：连续调试 ≥ 6 小时，或用户语气熵显著上升，或已出现 5 次以上的重试循环。
- **降级路径**：CRS 强制切入止损模式。唯一合法行动：请求外部视角、或建议休息 15 分钟后刷新 L1 证据链。疲劳状态下任何"再试一个修复"都是伪进展（M7）。

### 场景 6：同一故障反复修不好，而"现场"是被自己的代码加工过的（v3.1 新增，反制"派生观测当现实"）

- **特征**：已改 ≥ 3 轮；每一轮的证据都来自**自建**台账/面板/状态码；症状**变形**而不是消失
  （从"A 失败"变成"B 失败"）；每一轮单看都"有依据"。
- **降级路径**：暂停一切修复，转入**观测补全**（M8）——
  ① 找**数字矛盾**（同一行两个字段打架 = 某字段语义与你以为的不同）；
  ② 找**上游原话**（不在本方源码里的文案必然来自上游，追它在中继的哪一层被丢掉）；
  ③ 造**单变量对照**（同环境同身份，一次只改一个变量；分钟级、可自动判读）；
  ④ 修复时**顺手把盲区变成仪器**（让原始错误帧进入默认留痕）。
- **代价与前提**：观察期"什么都没修"，对急着恢复的用户要先讲清并给时间盒。
- **判据**：能一句话说出"**这次故障的原始报文长什么样**"吗？说不出就还在解释层里转。

### 场景 7：改进做在了"开发副本"上，而生效的是另一份（v3.1 新增，反制"部署漂移"）

- **特征**：同一份资产在磁盘上有多份副本（开发目录 / 实际加载目录 / 归档目录）；
  你确信"上次已经补过这一条了"，但**当前会话加载到的版本里没有它**。
- **降级路径**：**先用哈希比对确认"生效的那份"是哪一份**（`Get-FileHash`），
  再核对"我改的那份"与"生效的那份"是否同一内容；两份不一致时，**改完必须同步到生效位置**。
  判定顺序：**生效位置 → 其内容哈希 → 与开发副本的差异行**，任何一步没做都不要下"已经改好了"的结论。
- **判据**：改动生效的判据不是"我改了文件"，而**是"加载路径读到的字节变了"**（同 M3 的指纹原则）。

---

## 结语

软件以有限且可知的方式失败。工作的目标不是让人眼花缭乱，而是*通过严谨的证据收集来缩小症状和根本原因之间的鸿沟*。速度来自于避免浪费性的回合；可靠性来自于验证每一步；合作来自于尊重用户的时间。

本框架不是死记硬背的食谱——它是三条公理和一台心智资源调度器组成的自调温引擎。当你有证据时，行动。当你没有证据时，测量。当你不知道测什么时，引入外部能量。当你累到分不清这三条时，停下来。这就是从第一性原理出发，在有限认知带宽下，与一个复杂系统作战的最优解。
